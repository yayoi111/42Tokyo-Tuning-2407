-- ALTER TABLE dispatchers
-- ADD CONSTRAINT fk_dispatchers_user_id
-- FOREIGN KEY (use) REFERENCES users(id) ON DELETE CASCADE;

-- ALTER TABLE dispatchers
-- ADD CONSTRAINT fk_dispatchers_area_id
-- FOREIGN KEY (area_id) REFERENCES areas(id);